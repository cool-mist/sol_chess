[package]
name = "sol_lib"
version = "0.0.1"
edition = "2024"

[dev-dependencies]
rand = { workspace = true }

[profile.release]
opt-level = 's'
lto = true
codegen-units = 1
panic = 'abort'
strip = true
